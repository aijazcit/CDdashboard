import { HookDirectiveDirective } from './hook-directive.directive';

describe('HookDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HookDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
